# 보안 규칙 배포 전 필수 설정 (반드시 읽어주세요)

이번 업데이트로 관리자 판별 방식이 바뀌었습니다.

- 이전: Firebase Authentication에 **로그인만 되어 있으면** 누구나 관리자 권한
- 지금: Firestore의 `admins` 컬렉션에 **본인 UID로 된 문서가 실제로 있는 사람만** 관리자 권한

이렇게 바꾼 이유는, 나중에 실수로(또는 테스트 목적으로) Firebase Authentication에
계정을 하나 더 만들었을 때 그 계정이 자동으로 수강신청자 개인정보나 공지사항
관리 권한을 갖게 되는 걸 막기 위해서입니다.

**이 설정을 하지 않으면 원장님 본인 계정도 관리자 화면에 로그인은 되지만
"권한이 없습니다" 같은 오류가 발생하며 데이터를 조회/수정할 수 없습니다.**
아래 순서대로 딱 한 번만 설정해 주시면 됩니다.

---

## 1단계. 원장님 계정의 UID 확인하기

1. [Firebase 콘솔](https://console.firebase.google.com/) 접속 → `joongang-homepage` 프로젝트 선택
2. 왼쪽 메뉴에서 **Authentication** 클릭
3. **Users** 탭에서 원장님이 로그인에 사용하는 이메일 계정을 찾습니다
4. 그 계정 행(row)의 **User UID** 열에 있는 긴 영문/숫자 문자열을 복사합니다
   (예: `aB3dEfGhIjKlMnOpQrStUvWxYz12`)

## 2단계. Firestore에 admins 문서 추가하기

1. 왼쪽 메뉴에서 **Firestore Database** 클릭
2. **데이터** 탭에서 **+ 컬렉션 시작** 클릭
3. 컬렉션 ID: `admins` 입력 → 다음
4. **문서 ID**란에 1단계에서 복사한 **UID를 그대로 붙여넣기**
   (자동 생성 ID를 쓰지 마시고, 반드시 UID를 직접 입력해야 합니다)
5. 필드는 아무거나 하나 추가하면 됩니다. 예:
   - 필드: `email`
   - 값: 원장님 로그인 이메일 주소
6. 저장

이제 `admins/{UID}` 문서가 존재하므로, 이 계정으로 로그인하면 관리자 권한이
정상적으로 인식됩니다. 관리자를 한 명 더 추가하고 싶다면 같은 방식으로
`admins` 컬렉션에 문서를 하나 더 추가(문서 ID = 그 사람의 UID)하면 됩니다.
반대로 권한을 회수하려면 해당 문서를 삭제하면 됩니다.

## 3단계. 보안 규칙 배포

1. Firebase 콘솔 → **Firestore Database** → **규칙** 탭
2. 이 프로젝트의 `firestore.rules` 파일 내용을 전체 복사해서 붙여넣기
3. **게시** 클릭
4. **Storage** → **규칙** 탭에서도 동일하게 `storage.rules` 내용을 붙여넣고 게시
   (Storage 규칙은 Firestore의 `admins` 컬렉션을 조회하는 방식이 **아니라**,
   로그인 계정의 이메일 주소를 직접 확인하는 방식입니다 — 아래 "자료실 업로드가
   안 될 때" 참고)

## 4단계. 확인

관리자 화면에 로그인해서 수강신청 목록, 공지사항 관리, 팝업 설정이 정상적으로
보이는지 확인해 주세요. 만약 "권한이 없습니다" 오류가 뜬다면:

- `admins` 컬렉션의 문서 ID가 실제 로그인 계정의 UID와 정확히 일치하는지 다시 확인
- Firestore 규칙이 실제로 "게시"되었는지 확인 (규칙 편집만 하고 게시를 안 누르면 적용 안 됨)

---

## 자료실 업로드가 안 될 때 (0%에서 멈추는 경우)

`storage.rules`는 Firestore의 `admins` 컬렉션을 조회하지 않고, **로그인 계정의
이메일을 규칙 파일에 직접 나열**하는 방식으로 관리자를 판별합니다.

```
function isAdmin() {
  return request.auth != null
    && request.auth.token.email in [
      'jiho1234@jahrd.co.kr'
    ];
}
```

(참고: 처음에는 Firestore와 동일하게 `admins` 컬렉션을 조회하는 방식으로
만들었었는데, 이 방식은 Firebase 콘솔에서 "Firestore·Storage 두 서비스 연결
권한"을 별도로 한 번 더 승인해야만 정상 작동합니다. 이 승인 단계를 깜빡 놓치기
쉽고, 놓치면 업로드가 오류 메시지도 없이 0%에서 멈춘 것처럼 보여서 원인을 찾기
어려웠습니다. 그래서 추가 설정이 필요 없는 이메일 확인 방식으로 바꿨습니다.)

**관리자 계정을 추가/변경하실 때**는 위 목록에 이메일 주소를 추가하고 다시
Storage 규칙을 게시(Publish)해 주셔야 그 계정도 업로드/삭제가 가능해집니다.

업로드가 여전히 안 된다면:
- Storage 규칙이 실제로 "게시"됐는지 확인 (붙여넣기만 하고 게시를 안 누르면 적용 안 됨)
- 로그인한 이메일 주소가 위 목록의 이메일과 정확히 일치하는지 확인 (대소문자 포함)
- 브라우저 개발자도구(F12) → Console 탭에 `storage/unauthorized` 같은 오류가
  찍히는지 확인하면 권한 문제인지 다른 문제인지 구분하기 쉽습니다

---

## 참고: 이번에 함께 개선된 사항 (자동 반영, 별도 설정 불필요)

- 수강신청 접수번호(YYMM-N) 발급 방식을 트랜잭션 기반으로 변경 — 동시에 여러 명이
  신청해도 접수번호가 겹치지 않습니다
- 수강신청 저장 성공/실패를 정직하게 확인 후 안내 (예전엔 느린 네트워크에서
  실제 저장 여부와 무관하게 "성공"으로 보일 수 있었습니다)
- 일반 방문자는 더 이상 수강신청 목록(`applications`) 구독을 시도하지 않음 —
  이전에는 모든 방문자 브라우저에서 매번 불필요한 권한 오류가 발생했습니다
- 공지사항/인기강좌 기본 데이터 시딩을 고정 문서 ID 방식으로 변경 — 동시 접속 시
  중복 생성 방지
- Storage 업로드에 파일 크기(10MB) 및 형식(이미지/PDF) 제한 추가

## 자료실(materials) 기능 추가 시 배포 체크리스트

자료실 기능을 위해 Firestore와 Storage 규칙에 새 경로가 추가됐습니다.
아래 항목도 3단계(보안 규칙 배포)와 함께 반드시 게시해 주세요.

- `firestore.rules`에 `materials` 컬렉션 규칙 추가됨 (읽기 공개, 쓰기 관리자 전용)
- `storage.rules`에 `materials/` 경로 규칙 추가됨 (업로드 최대 100MB, 관리자 이메일만 허용)
- 관리자 화면 "자료실 관리" 탭에서 파일을 업로드하면 자동으로 `materials/{과정명}/`
  경로에 저장되고, 공개 자료실 페이지(`/#materials`)에 바로 노출됩니다
- 채점프로그램(.exe) 등 실행 파일은 브라우저 보안 경고를 피하기 위해 `.zip`으로
  압축해서 올리는 것을 권장합니다 (업로드 화면에도 안내 문구가 있습니다)
- 파일이 큰 자료(설치 프로그램 등)를 학생들이 자주 내려받으면 Firebase Storage의
  일일 무료 다운로드 용량(1GB)을 초과해 Blaze 요금제 과금이 발생할 수 있습니다.
  자주 받는 대용량 파일이 많아지면 Firebase 콘솔의 사용량을 가끔 확인해 주세요

## 수강생 회원가입/승인 기능 (자료실 접근 제한)

자료실이 아무나 접근 가능한 공개 페이지였던 것을 "회원가입 + 원장님 승인"이
필요한 구조로 바꿨습니다.

- 수강생이 이름/연락처/이메일/비밀번호로 회원가입하면, Firestore
  `students/{UID}` 문서가 `status: '승인대기'`로 자동 생성됩니다
- 원장님은 관리자 화면의 새 탭 **"수강생 승인 관리"**에서 승인/거절을 처리합니다
- 승인된 수강생만 자료실 목록을 볼 수 있고 다운로드할 수 있습니다
- 관리자 계정으로 로그인하면 별도 승인 없이 자료실을 그대로 볼 수 있습니다

**알아두시면 좋은 점**

- 수강생 로그인과 관리자 로그인은 같은 Firebase Authentication을 공유해요.
  다만 브라우저 하나에는 한 번에 한 계정만 로그인된 상태를 유지할 수 있어서,
  같은 브라우저에서 수강생 계정으로 로그인한 채로 관리자 화면을 열면
  관리자 로그인 화면이 다시 뜹니다(정상 동작이에요, 다른 계정이니까요).
  원장님 본인 컴퓨터에서는 크게 문제되지 않지만, 헷갈리시면 관리자 업무는
  시크릿 창(비공개 창)에서 진행하시는 걸 권장드려요
- 수강생 정보(이름/연락처/이메일)는 `students` 컬렉션에 저장되며, 이 데이터도
  개인정보이므로 조회는 본인과 관리자만 가능하도록 규칙으로 막아뒀습니다
- Storage(실제 파일)는 관리자 로그인 이메일(`jiho1234@jahrd.co.kr`)만 직접
  읽을 수 있도록 막혀 있습니다. 승인된 수강생이라도 Storage를 직접 읽지
  않으며, 자료 다운로드는 항상 `api/download-material.ts`(서버,
  Firebase Admin SDK)를 거칩니다. 이 서버 함수가 요청마다 Firestore의
  최신 승인 상태(`students.status`)와 자료 유형(`materials.studentVisible`)을
  다시 확인한 뒤 5분짜리 임시(Signed) URL을 발급하는 구조라, 승인 상태가
  바뀌면 다음 요청부터 즉시 반영됩니다(재로그인이나 재승인 같은 별도
  조치가 필요 없습니다)

## '학원서식' 유형은 승인된 수강생에게도 안 보여요

학원 내부용 서류 성격의 자료(예: 학원서식)는 승인된 수강생에게도 노출되지
않고 관리자만 조회할 수 있도록 Firestore 규칙에서 막아뒀습니다
(`materials` 컬렉션 읽기 규칙에 `materialType != '학원서식'` 조건 추가).
수강생 화면의 자료 유형 필터도 실제로 조회 가능한 자료가 있는 유형만
동적으로 표시되므로, 학원서식 필터 버튼 자체가 수강생 화면에는 나타나지
않습니다.

⚠️ Firestore(목록 조회) 레벨에서는 확실히 막혀 있고, Storage(실제 파일)
레벨에서는 애초에 관리자 이메일만 직접 읽기가 허용되어 승인된 수강생도
Storage에 직접 접근할 수 없습니다(위 "알아두시면 좋은 점" 항목 참고).
자료 다운로드는 반드시 `api/download-material.ts` 서버를 거치고, 이
서버가 매 요청마다 자료 유형(`materialType != '학원서식'`)을 다시
확인하므로, 학원서식 파일은 승인된 수강생이라도 링크를 직접 알아내도
접근할 수 없습니다.

## 관리자가 수강생 계정 정보를 삭제(리셋)할 수 있어요

관리자 화면의 "수강생 승인 관리" 탭에서 각 수강생 옆의 휴지통 아이콘으로
등록 정보를 삭제할 수 있습니다. 삭제하면:

- Firestore의 `students/{UID}` 문서(이름/연락처/이메일/승인상태)가 삭제됩니다
- 그 계정으로 다시 로그인해도 "등록된 계정 정보가 없어요" 화면이 뜨고,
  자료실을 이용하려면 처음부터 다시 회원가입해야 합니다

⚠️ **알아두실 점**: 이 삭제는 Firestore에 저장된 프로필 정보만 지웁니다.
Firebase Authentication의 로그인 계정 자체(이메일 주소, 비밀번호)는 브라우저
권한으로는 다른 사람 계정을 지울 수 없어서 그대로 남아있습니다. 즉 같은
이메일로 다시 로그인은 되지만, 프로필이 없으니 다시 회원가입 절차를 거쳐야
자료실을 쓸 수 있는 구조입니다. 로그인 계정 자체까지 완전히 삭제하려면
Firebase Admin SDK 기반 서버 기능(서비스 계정 키 필요)이 별도로 필요합니다.

### 왜 이 부분이 중요했는지 (버그 수정 배경)

이번에 수강생 계정 삭제 기능을 만들면서, 기존 로직에 실제 보안 허점이
있었다는 걸 발견해서 함께 고쳤습니다. 예전에는 "students 문서가 없으면
관리자 계정으로 간주해 자료실을 통과시키는" 방식이었는데, 이 로직대로면
관리자가 수강생 정보를 삭제한 뒤 그 사람이 다시 로그인했을 때 오히려
"학생 문서 없음 = 관리자"로 잘못 인식되어 **승인 없이 자료실이 뚫리는**
문제가 생길 뻔했습니다. 지금은 `admins` 컬렉션 등록 여부를 명시적으로
확인해서, 관리자 계정과 "정보가 삭제된 일반 계정"을 정확히 구분합니다.

## 관리자 계정은 삭제할 수 없어요 (안전장치)

수강생 삭제 기능(`api/delete-student.ts`)은 대상 계정이 `admins` 컬렉션에
등록된 관리자인 경우 서버에서 요청 자체를 거부하도록 되어 있습니다. 지금은
관리자 화면에 관리자 삭제 버튼이 아예 없지만, 요청을 실제로 처리하는
서버에서도 한 번 더 확인해두면 나중에 관리자 계정이 여러 개로 늘어나거나
API가 예상치 못한 방식으로 호출되더라도 안전합니다.

## 수강생 완전 삭제 기능 서버 설정 (선택 사항이지만 강력 권장)

관리자 화면에서 수강생을 삭제하면, 기본적으로는 Firestore의 프로필 정보만
지워집니다. 이 상태로는 **같은 이메일로 다시 회원가입하려고 하면 "이미
가입된 이메일입니다" 오류**가 나요 — Firebase Authentication의 로그인
계정 자체는 브라우저 권한으로는 지울 수 없어서 남아있기 때문입니다.

이 문제까지 완전히 해결하려면, 서버(Vercel 서버리스 함수)에서 관리자
권한으로 로그인 계정 자체를 지울 수 있게 아래 설정을 한 번 해주셔야 해요.

### 설정 방법

1. Firebase 콘솔 → 왼쪽 상단 **⚙️ 설정** → **프로젝트 설정**
2. **서비스 계정** 탭 클릭
3. **새 비공개 키 생성** 버튼 클릭 → JSON 파일이 다운로드됩니다
   (⚠️ 이 파일은 계정 전체를 관리할 수 있는 매우 민감한 정보입니다.
   절대 GitHub 등 공개된 곳에 올리지 마세요)
4. 다운로드한 JSON 파일을 텍스트 편집기로 열어서 **내용 전체를 복사**
5. Vercel 대시보드 → 이 프로젝트 → **Settings** → **Environment Variables**
6. 새 환경변수 추가:
   - Key: `FIREBASE_SERVICE_ACCOUNT_KEY`
   - Value: 방금 복사한 JSON 내용 전체를 그대로 붙여넣기 (한 줄로 붙여넣어도 됩니다)
   - Environment: Production(운영), 필요하면 Preview/Development도 함께 체크
7. **Save** 클릭 후, Vercel에서 **재배포**(Redeploy)해야 새 환경변수가 적용됩니다
   (환경변수는 재배포 시점에만 반영돼요)

### 이 설정을 안 하면 어떻게 되나요?

설정을 안 해도 삭제 버튼 자체는 정상 작동해요. 다만:
- Firestore 프로필만 지워지고, 로그인 계정은 남아있는 상태가 됩니다
- 삭제 시 화면에 "로그인 계정 자체는 그대로 남아있어요"라는 안내가 뜹니다
- 그 계정으로 재가입하려면, Firebase 콘솔 → Authentication → Users에서
  해당 이메일 계정을 관리자가 수동으로 찾아 직접 삭제해야 합니다

## 전화번호 중복 가입 방지

같은 전화번호로 이메일만 바꿔가며 여러 계정을 만드는 것을 막았습니다.

- Firestore에 `phoneRegistry` 컬렉션이 새로 생겼습니다. 문서 ID가 전화번호
  (숫자만, 예: `01090791234`)이고, 그 번호로 가입한 계정의 UID가 들어있습니다
- 이미 등록된 번호로 가입을 시도하면 "이미 등록된 연락처입니다" 오류가 뜨고
  가입이 막힙니다
- 관리자가 "전체 계정 관리"나 "수강생 승인 관리"에서 계정을 삭제하면,
  연결된 `phoneRegistry` 잠금도 함께 해제되어 같은 번호로 재가입할 수 있어요
  (이메일 재가입 문제와 동일한 원리로 미리 처리해뒀습니다)

## 이메일 실소유 확인 (이메일 인증)

가입 시 입력한 이메일 주소가 형식만 맞는 게 아니라 실제 본인이 받을 수 있는
주소인지 확인하는 절차를 추가했습니다.

- 회원가입 완료 직후 Firebase가 자동으로 인증 메일을 발송합니다
- 수강생이 받은편지함에서 인증 링크를 클릭하기 전까지는, 로그인은 되지만
  "이메일 인증이 필요해요" 화면만 보이고 승인 대기 단계로 넘어가지 않아요
- "인증 완료했어요, 확인하기" 버튼을 누르면 인증 여부를 다시 확인하고,
  "인증 메일 다시 받기"로 재전송도 가능해요
- 관리자 계정은 이 절차와 무관하게 그대로 통과됩니다

⚠️ **참고**: Firebase의 기본 발신 이메일 주소(`noreply@joongang-homepage.firebaseapp.com`
형태)로 메일이 발송돼요. 학원 자체 도메인 이메일로 보내고 싶으시면 Firebase
콘솔 → Authentication → Templates에서 발신자 설정을 변경할 수 있지만,
별도의 도메인 인증 절차가 필요해 지금은 기본값 그대로 두었습니다.

## 비밀번호 찾기

이 시스템은 별도의 "아이디"가 없고 이메일 자체가 로그인 아이디라서,
"아이디 찾기"는 필요 없고 "비밀번호 재설정"만 제공합니다.

- 로그인 화면에 "비밀번호를 잊으셨나요?" 링크가 있어요
- 가입한 이메일을 입력하면 Firebase가 비밀번호 재설정 링크를 메일로 보내줘요
- 보안을 위해, 존재하지 않는 이메일을 입력해도 동일하게 "메일을 보냈다"는
  메시지가 뜹니다 (실제 가입 여부를 외부에 알려주지 않기 위함이에요)
- 이메일 발송 자체는 위의 "이메일 실소유 확인"과 같은 Firebase 발신
  이메일 주소를 사용합니다

## 자료실 다운로드 방식 변경 (중요한 보안 수정)

자료실 파일 다운로드 방식을 완전히 바꿨습니다. 예전 방식에는 실제 보안
허점이 있었어서 반드시 알아두셔야 해요.

**예전 방식(문제가 있었던 방식)**: 파일을 업로드할 때 Firebase Storage의
`getDownloadURL()`로 "다운로드 토큰이 포함된 영구 링크"를 만들어서
Firestore에 저장해두고, 승인된 수강생이 그 링크를 그대로 썼습니다.

**문제**: 이런 토큰 포함 링크는 Storage 보안 규칙 자체를 우회하는 Firebase의
특성이 있어요. 즉 그 링크를 아는 사람은 로그인 여부·승인 여부와 무관하게
파일을 받을 수 있었습니다. 승인된 수강생이 그 링크를 캡처하거나 다른
사람에게 전달하면, 받은 사람은 로그인조차 필요 없이 파일에 접근할 수
있었던 거예요.

**지금 방식**: Firestore에는 더 이상 다운로드 링크를 저장하지 않습니다.
대신 다운로드 버튼을 누를 때마다 새 서버리스 함수(`api/download-material.ts`)가:

1. 요청한 사람이 실제로 로그인했는지
2. 관리자이거나 승인된 수강생인지
3. '학원서식'처럼 수강생에게 금지된 유형은 아닌지

를 매번 다시 확인한 뒤, **5분 안에 만료되는 임시 링크**를 새로 발급합니다.
링크가 어딘가에 유출되더라도 5분 후엔 무효화되고, 애초에 승인되지 않은
사람은 이 링크를 받을 수조차 없습니다. 다운로드 횟수 집계도 이제 이
서버 함수가 직접 처리해서, 수강생이 클라이언트에서 직접 숫자를 조작할
수 있는 경로도 없앴습니다.

⚠️ 이 API도 `FIREBASE_SERVICE_ACCOUNT_KEY` 환경변수(수강생 완전 삭제
기능과 동일한 서비스 계정 키)가 필요합니다. 이미 설정하셨다면 별도로
하실 일은 없어요.

## 자료실 조회 쿼리 구조 변경 (중요한 정합성 수정)

승인된 수강생이 자료실을 조회할 때, **컬렉션에 '학원서식'이 하나라도
있으면 조회 자체가 통째로 거부되어 자료실이 텅 비어 보이는** 문제가
있었습니다. 이건 개인정보가 새는 보안 문제는 아니었지만, 시스템이
아예 오작동하는 심각한 문제였습니다.

**원인**: Firestore 보안 규칙은 "필터"가 아니라 "쿼리가 반환할 수 있는
모든 문서가 규칙을 통과해야만 쿼리 전체가 허용되는" 방식으로 동작합니다
(Firestore 공식 문서 표현). 예전 규칙은 `resource.data.materialType !=
'학원서식'`처럼 문서마다 값이 다른 조건으로 걸러내려 했는데, 정작
수강생이 보내는 조회 쿼리에는 그 조건과 짝을 이루는 제약이 없어서,
컬렉션에 학원서식이 하나라도 섞여 있으면 Firestore가 "이 쿼리는 규칙을
못 지킬 수도 있다"고 판단해 조회 전체를 거부했습니다.

**해결**: 자료 문서에 `studentVisible`이라는 불리언 필드를 새로 추가했고
(학원서식이면 false, 그 외엔 true), 수강생용 조회 쿼리에 반드시
`where('studentVisible', '==', true)` 조건을 포함시켜서, Firestore가
"이 쿼리 결과는 전부 studentVisible == true"임을 미리 보장할 수 있게
했습니다. 관리자용 조회(자료실 관리 탭)는 이 제약 없이 전체를 그대로
봅니다.

⚠️ **마이그레이션 관련 주의사항**: 이 필드는 앞으로 새로 업로드하는
자료부터 자동으로 저장돼요. **이 업데이트 전에 이미 업로드했던 자료는
이 필드가 없어서, 학원서식이 아니더라도 수강생 화면에는 안 보일 수
있어요.** 기존 자료가 있다면 삭제 후 재업로드해 주시면, 새 방식으로
다시 저장되어 정상적으로 보입니다.
